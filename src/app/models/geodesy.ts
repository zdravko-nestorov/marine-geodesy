export class Geodesy {
  id: number;
  y: string;
  x: string;
  height: string;

  constructor(id: number, y: string, x: string, height: string) {
    this.id = id;
    this.y = y;
    this.x = x;
    this.height = height;
  }
}
